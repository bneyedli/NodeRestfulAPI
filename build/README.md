```
$ cd build/
$ ls -1
artifacts # Artifacts generated by $(BUILD_TARGET) are output here as artifacts/$(BUILD_TARGET)
buildspec.yml # AWS CodeBuild project spec
docker-mod-nginx-modsecurity # Docker job to build and package modsecurity module for nginx
docker-node-rest-api # Our API Image
docker-ubuntu-base # Baseline ubuntu image
docker-ubuntu-web # Baseline ubuntu web (nginx) image
Makefile # Top level make that drives build automation
utils # Supplemental build scripts
```
